# Required by the active MiniMax-only production profile. Never commit values.
# MiniMax Token Plan key (sk-cp-*).
MINIMAX_API_KEY=
# Optional: only used by explicit Kimi profiles. Use a Platform key with
# runs/production-k2.6.yaml, or an sk-kimi-* Code membership key with
# runs/production-kimi-code.yaml. Kimi is disabled in runs/production.yaml.
KIMI_API_KEY=
ANTHROPIC_API_KEY=
