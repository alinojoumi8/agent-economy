# Required only for real-provider profiles. Never commit populated values.
# MiniMax Token Plan key (sk-cp-*).
MINIMAX_API_KEY=
# Kimi credential for the selected profile. Use a Platform key with
# runs/production.yaml, or an sk-kimi-* Code membership key with
# runs/production-kimi-code.yaml. The credential types are not interchangeable.
KIMI_API_KEY=
ANTHROPIC_API_KEY=
