# Agent Economy — runtime dependencies
# Core simulation kernel uses only the Python standard library (sqlite3, asyncio, json, ...).
# The API server and real LLM adapters need the packages below.
fastapi>=0.110
uvicorn[standard]>=0.51.0
websockets>=12
PyYAML>=6
httpx>=0.27
pydantic>=2
# dev / test
pytest>=8
