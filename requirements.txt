# Agent Economy — runtime dependencies
# Core simulation kernel uses only the Python standard library (sqlite3, asyncio, json, ...).
# The API server and real LLM adapters need the packages below.
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
PyYAML>=6
httpx>=0.27
pydantic>=2
python-dotenv>=1.0
# Hosted R22 control plane, durable artifacts, and operational metrics.
psycopg[binary,pool]>=3.2,<3.4
boto3>=1.35,<2
prometheus-client>=0.21,<1
# dev / test
pytest>=8
