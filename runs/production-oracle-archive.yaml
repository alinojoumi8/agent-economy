# Frozen provider baseline for immutable Oracle calibration campaigns v1-v9.
# The resolved values are receipt-bound; current live routing belongs in
# production.yaml and must not retroactively alter historical commitments.
extends: base-oracle-archive.yaml

population:
  size: 63

budget:
  cap_usd: null

llm:
  concurrency: 3
  provider_retries: 1
  providers:
    minimax:
      kind: openai_compat
      base_url: https://api.minimax.io/v1
      api_key_env: MINIMAX_API_KEY
      prompt_cache_mode: provider_automatic
      timeout_s: 180
      healthcheck_path: /models
      max_tokens_field: max_completion_tokens
      request_defaults:
        reasoning_split: true
        max_completion_tokens: 4096
    kimi:
      kind: openai_compat
      base_url: https://api.kimi.com/coding/v1
      api_key_env: KIMI_API_KEY
      prompt_cache_mode: "off"
      timeout_s: 180
      healthcheck_path: /models
      max_tokens_field: max_tokens
      request_defaults:
        reasoning_effort: medium
        temperature: 1.0
        max_tokens: 4096
  default_route: {provider: minimax, model: MiniMax-M3}
  routes:
    citizen:        {provider: minimax, model: MiniMax-M3}
    founder:        {provider: minimax, model: MiniMax-M3}
    central_banker: {provider: kimi, model: kimi-for-coding}
    credit_officer: {provider: kimi, model: kimi-for-coding}
    reporter:       {provider: kimi, model: kimi-for-coding}
    editor:         {provider: kimi, model: kimi-for-coding}
    vc_partner:     {provider: kimi, model: kimi-for-coding}
    oracle:         {provider: kimi, model: kimi-for-coding}
