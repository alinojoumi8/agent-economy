# Fresh release corpus after v8 seed 7375 exhausted the Kimi account quota.
# V8 remains excluded diagnostic evidence. No prior response, claim,
# checkpoint, replay, receipt, profile ancestry, manifest entry, run identity,
# artifact, or seed is reused.
extends: ../acceptance/rehearsal.yaml
seed: 7381
acceptance:
  min_ticks: 335
  max_spend_usd: 25
  efficiency_target_usd: 25
  oracle_campaign_id: oracle-calibration-v9
  oracle_campaign_version: 9
  oracle_min_latency_samples: 6
  required_shocks: []
  require_oracle_scoring: true
  require_experiment: false
  require_phenomena: false
budget:
  cap_usd: 25
  oracle_reserve_usd: 25
  conversation_pairs: 0
llm:
  concurrency: 1
  # MiniMax's standard <=512k PAYG tier publishes $0.30/M input,
  # $1.20/M output, and $0.06/M automatic prompt-cache reads.
  pricing:
    MiniMax-M3: {in: 0.30, out: 1.20, cache: 0.06}
  routes:
    oracle: {provider: minimax, model: MiniMax-M3}
lifecycle:
  population_mode: stable
  arrival_delay_min: 5
  arrival_delay_max: 20
shocks: []
