# Environment recovery after cleanup

The user confirmed that machine cleanup finished and authorized restoring the
tools needed to continue. Ubuntu was reinstalled as the WSL distribution
`Ubuntu` using Ubuntu 26.04 LTS. This is an environment recovery within the
existing implementation goal. Goods and equity research retain equal priority.

## Preserved state

The native campaign and Windows candidate continuation both became terminal
failures while their dependencies and source copies disappeared. Their original
failed receipts remain unchanged. Earlier validation descriptions are historical
observations; missing artifacts cannot establish current release admission.

Durable recovery evidence is under
`data/recovery/environment-cleanup-69dbcab9/`, excluded from Git. It contains:

- A transaction-consistent emergency snapshot at committed day 11,941, with
  day 11,942 in progress, and its preservation receipt.
- A separate, closed day-11,998 database without SQLite sidecars. Its 5,234,458,624
  bytes match the closed source: SHA-256
  `95632b39aa084f7fa7c3cbf7686d17551bebce8ac221c867d661c3724204a334`.
- The original native configuration, contract, controller and frozen-source
  manifest, plus surviving terminal native and Windows validation records.

The native parent's last accepted segment ended at day 11,756. The child advanced
to day 11,998 and closed its world, but final source verification failed after
`.claude/launch.json` disappeared. The independently preserved day-11,998 boundary
is a recovery candidate, not a successful original segment receipt.

All 1,096 surviving native source files matched the original manifest. Three
configuration files were absent: `.claude/launch.json`, `.github/dependabot.yml`
and `.github/workflows/ci.yml`. The first two can be recovered byte-for-byte from
the working project. The original CI workflow is not currently recoverable.
Every economic engine, agent, world and runtime file remains available. A new
recovery source must disclose the missing non-runtime workflow, preserve its
expected hash, and verify every admitted file. Do not weaken the original
controller or relabel its failed receipt.

## Restored tools

Ubuntu has managed Python 3.11.15 and 3.12.13, installed through pinned uv 0.12.1.
Their environments use the project's hash-locked dependencies under
`/home/matri/.local/share/agent-economy/`. Both load the test, API and research
packages successfully. Ubuntu's system Python is only a tool installer.

Windows retains the main project's Python 3.11.15 environment. Separate recovery
environments restore native Python 3.11.15 from the native run's original lock
and current Python 3.12.13 from the working project's lock. Their dependency
checks pass. Do not reuse the partially deleted `.codex/tmp` environments.

## Recovery execution contract

1. Preserve terminal records and classify missing evidence before execution.
   Old live-process pointers must become terminal recovery pointers.
2. Audit the closed day-11,998 copy read-only: database structure, committed
   boundary, census continuity, household membership, full ledger entries,
   provider identity and zero recorded paid cost. Keep the preserved source
   unchanged. Bound the audit prospectively and retain its outcome.
3. If admission passes, continue a separate working copy with the exact surviving
   native runtime, Semantics 20, seed 1, the original configuration and scripted
   providers. Keep the same run identity and original 14,600-day target. Account
   separately for the interruption; do not fabricate an earlier passing segment.
4. The native campaign already consumed 18,950.218 of its 28,800 seconds.
   Its remaining allowance is 9,849.782 seconds before any conservatively charged
   recovery validation. Retain 600-second clean segments, the 16 GiB source/RSS
   ceilings, 20 GiB available-RAM admission and 40 GiB free-disk floor. Preserve
   the original replay, comparison, export and readback limits. Failure is
   terminal for the new invocation; no silent retry or budget reset.
5. Restore and validate the replay/export reader separately. The deleted analysis
   copy and its old smoke receipts are not a ready verifier. Full-prefix exact
   replay and independent export/readback remain required.
6. Rebuild the private population registration candidate from an explicitly
   captured source and the saved revision-2 patch. Preserve all historical
   terminal results, but do not transfer passing coverage from a missing
   provenance chain. Reconcile the original 3,339-node inventory, restored
   collection, declared platform requirements and remaining resource allowances
   before new execution. The Windows broader-suite work already consumed
   2,392.671 of its 14,400 seconds, including earlier diagnostic charges.
7. Keep new proof records outside temporary fixture directories. Any prospective
   fixture disposal must be declared before a new test batch and require exact
   collection, complete passing setup/call/teardown, unchanged evidence and all
   owned process identities closed. Failed, skipped or interrupted cases retain
   their fixtures. This does not authorize further historical artifact cleanup.

## Release boundary

Schema 25, maximum Semantics 20 and unregistered migration 26 remain in the main
project. Native acceptance, the rebuilt candidate's platform gates and W5 remain
open. Apply the saved registration patch only after admission; then proceed
through W6 education, W7 production/housing, W8 banking and W9 validation in the
existing order. Do not modify the economy to force native employment or trading
outcomes into a favorable result.

The restored environment and preservation copies do not by themselves constitute
a passing simulation or release gate. Append executed recovery results below.

## Executed recovery results

The closed day-11,998 admission passed in 15.547 supervised seconds. Database
quick-check and foreign-key checks pass; full ledger-entry sums agree with every
account and the cached ledger totals. All transactions balance. The household
checks pass and all 11,999 census rows form a continuous, reconciled sequence.
All 228,264 recorded model calls use the scripted provider at zero cost. The
database hash, size and modification time remain unchanged, with no sidecars.

The restored native source has 1,098 hash-verified files. Only the explicitly
recorded historical CI workflow remains absent. A new controller continues a
separate copy, charges the audit against the original native allowance and
retains the original failed receipt. Its maximum remaining duration is
9,834.235 seconds, including its own preparation and final checks. Original and
new native working artifacts share the original 16 GiB ceiling; preservation
snapshots remain separately accounted recovery evidence. The first resumed day
closed at 11,999 with ledger and household checks passing. Subsequent clean
segments are running; full native acceptance remains pending.

The Windows v4 phase audit retains 546 complete passes, four skips, two failures
and 1,888 unproved cases from its 2,440 selected cases. The two failures report
missing Oracle profile files during cleanup. These are historical phase results,
with no passing coverage carried into the rebuilt candidate because its source
and earlier proof chain were removed.

The new private candidate captures 1,198 files and applies only the saved
15-file revision-2 patch. Its Git commit is
`ca6c1cf6e98356994e6bba1277fc9d03b2fa223d`; its research source hash is
`69f1f6e9ac63b564cc56ab224af342c9c2cd4e51a112f810e219943083b830cf`.
Windows and Ubuntu copies produce that same identity. The original pytest
capture plugin was restored byte-for-byte from its retained generator.

Prospective fixture-retention probes pass on both operating systems: 11 cases
each, including failure, skip, interruption, changed proof, escaping links, live
processes and missing phase evidence. Each probe removes only its newly created
8 KiB successful fixture and preserves all refusal cases and its unrelated
sentinel. The Windows probe took 9.031 seconds; Ubuntu took 5.772 seconds.

All four restored interpreters passed the project's five-file, 144-case smoke
selection: 576 passing test executions, with every setup, call and teardown
recorded and no skipped cases.

| Environment | Passed | Supervised seconds |
| --- | ---: | ---: |
| Windows / Python 3.11.15 | 144 | 204.094 |
| Windows / Python 3.12.13 | 144 | 193.343 |
| Ubuntu / Python 3.11.15 | 144 | 112.505 |
| Ubuntu / Python 3.12.13 | 144 | 115.079 |

Each invocation stayed within its 600-second cap, 2 GiB process-tree limit,
4 GiB fixture ceiling and 40 GiB free-space floor. At most two test invocations
ran together. Complete source/phase/process proof preceded removal of their
successful temporary fixtures. Logs, collection, phases, source manifests,
outcomes and fixture inventories remain, including copies of the Ubuntu proof
in the Windows recovery directory. These focused checks do not replace the
remaining full platform matrix.

The original Gitleaks 8.30.1 scanner and Node 22.23.2 are also restored on
Windows and Ubuntu from checksum-verified release archives. The Windows scanner
matches its prior recorded binary hash. Their version checks pass; the machine's
default Node 24 installation remains available. Docker responds as version
29.7.2. This establishes tool availability, not a new hosted or dashboard gate.

Operational handoff: `tmp/environment-recovery-active.json`. Source manifests,
terminal records, admission proof, restoration receipts and the new native
contract live in the durable recovery directory named above.

## Full candidate inventory and continuation

Unfiltered collection on all four restored environments reconciles exactly
3,339 cases, with no missing, additional or duplicate identities. Collection
took 14.094 / 15.954 supervised seconds on Windows 3.11 / 3.12 and
10.372 / 9.889 seconds on Ubuntu 3.11 / 3.12. Collection executes no tests.

The next platform gate retains the newly verified 144 smoke cases and executes
the remaining 3,195. It maps positions in the complete fresh collection to the
same 16-way round-robin scheme used by CI, then removes the proved smoke cases.
This is a rebuilt-source validation invocation; deleted provenance contributes
no passing cases. Original failed attempts and their resource charges remain.

Begin with Windows Python 3.11 using two workers. Its 11,775.110-second allowance
deducts the original 2,392.671 seconds, the 9.031-second retention probe,
204.094-second smoke and 14.094-second full collection from the original
14,400 seconds, with five seconds held back. Each new CI-position group has an
1,800-second cap inside that shared remaining allowance. This new group map is
declared explicitly; it does not transfer earlier group success or rewrite
their terminal receipts. Each worker has a 2 GiB process-tree ceiling, so the
two workers stay within 4 GiB combined.

The new Windows artifact ceilings deduct the terminal v4 artifacts from its
remaining caps: 60,533,905,180 logical bytes and 16,583,525,223 allocated bytes.
Fresh short fixture directories inherit NTFS compression and use the already
exercised rename-safe allocation observer. Keep 104 GiB host space reserved for
the native run, separate verifier and free-space floor. Only wholly passing
batches may dispose of their prospectively temporary fixtures after proof.

The eight named PostgreSQL/S3 integration cases retain their ordinary explicit
environment skips in this general suite. Such batches keep their fixtures.
These skips must be reported, and a separate configured Docker integration gate
remains required. Any unexpected skip, failed phase, source change, resource
stop or incomplete collection makes the new invocation fail visibly. It does
not retry itself. Later platform invocations require their own remaining-time
accounting and verified source identity before launch.

## Restored replay and export components

A separate analysis source restores all 1,197 files from the captured main-tree
manifest, byte-for-byte. The native runtime remains its separate 1,098-file
recovery source. The retained replay/export stage helper, life-course auditor,
cohort validator and family-consent validator all match their original recorded
hashes; their old fixed directory references are supplied as explicit new local
runtime paths by the rehearsal wrapper.

The three-day rehearsal completed in 50.875 seconds under a 600-second combined
limit and 120 seconds per stage. It generates a fresh scripted native fixture,
replays in a separate native process, compares through the restored analysis
reader, exports, then independently reads the bundle and audits life-course
records. All owned processes closed. All 189 comparison tables match exactly.
The export and independent readback agree on 193 tables and 2,752 rows. Replay
consumed all 58 expected recorded calls exactly once, with no missing or extra
calls, no compatibility fallback and no fresh provider dispatch.

This verifies the restored components, not the final campaign controller or the
14,600-day source. The future full controller must admit the terminal native
recovery receipt and unchanged closed source, preserve original verification
ceilings, and join the saved report lineage before running the auditor. All 32
annual household reports before cleanup survive in the terminal archive; new
annual reports belong to the recovery continuation. The prospective complete
report inventory is days 365 through 14,600 at 365-day intervals. Never invent
missing historical reports or rewrite the native source to satisfy that check.

The component closure is `restored-verifier-smoke-closure.json` in the recovery
directory. The full Windows 3.11 gate and native recovery remain active under
their existing controllers; their final verdicts and the later platform gates
remain pending.

## Final campaign verifier admission

The final reusable verifier is now rehearsed. Its first wrapper rehearsal passed
in 51.203 seconds. Revision 2 adds a combined artifact ceiling, a hash-bound stage
specification/owner relationship and final source/storage checks. Its separate
rehearsal passed in 54.359 seconds, including all four verification stages and
the life-course/report comparison. The earlier passing rehearsal remains saved.

The campaign entry point also rejected the currently running native trial before
creating a verification directory or launching a child. Full verification may
start only after the recovery reaches day 14,600, closes its owned processes and
retains a matching database hash, original cumulative-time accounting and all
40 annual household reports. Report copies retain their original or recovery
origin and hashes; the preserved source remains read-only. Temporary replay
snapshots are directed inside the private allocation so their storage is counted.

Campaign limits remain 28,800 seconds for replay, 7,200 for comparison, 7,200 for
export and 3,600 for independent readback, including the life-course audit.
Private, replay and export allocations each stay below 16 GiB, with a combined
48 GiB ceiling including copied reports and proof metadata. The memory ceiling
is 16 GiB, with 20 GiB available RAM required at admission and a 40 GiB disk floor.
Failures remain terminal for each invocation.

The executable handoff is the hash-pinned local
`tools/restored_campaign_verifier_v2.py` under the recovery directory, invoked
with `--case campaign` after admission. Its smoke receipt is
`campaign-verifier-smoke-v2/receipt.json`; the negative admission record is
`campaign-live-source-rejection.json`. The full campaign has not been verified.

W5 still follows the original S5 contract. As clarified in the
[native employment diagnosis](2026-09-10-native-employment-diagnosis.md), adulthood
does not guarantee employment or wealth. Absent native employment remains a
reported coverage/outcome limitation; it must not be converted into forced jobs
or treated as an accounting failure. The broader education-to-work demonstration
and empirical validation remain in the later work packages.

## Remaining Ubuntu full suites prepared

The full-suite controller and historical budget record have now been copied to
the restored Ubuntu recovery directory and verified byte-for-byte. All 1,198
candidate source files still match the frozen manifest. Both Ubuntu interpreters,
the pinned scanner and Node runtime respond successfully. The separate complete
collections each contain 3,339 unique cases, including the 144 proved smoke cases;
each full invocation therefore selects the remaining 3,195 cases.

The calculated remaining time is 14,266.352 seconds for Ubuntu Python 3.11 and
14,270.032 seconds for Ubuntu Python 3.12. These are separate invocations with
the existing two-worker limit, 64 GiB artifact ceiling and 104 GiB host reserve.
Admission requires that reserve plus the artifact allowance (168 GiB free).
Preparation observed about 410.6 GiB free. Admission must recheck current space.

Neither Ubuntu full suite has started. Finish and reconcile the active Windows
3.11 invocation first; do not overlap full platform invocations. Exact commands,
source verification count, controller hashes and budget calculations are saved
in `linux-full-gate-readiness.json` under the recovery directory. Preparation
does not count as full-suite success or admission of the population changes.

## Dashboard recovery attempt

A separate Ubuntu source copy was verified against all 1,198 candidate files
before running the dashboard build checks. Dependency installation passed:
125 packages installed, 126 audited, with no reported vulnerabilities. The
subsequent complete Node unit-test selection, with one test-file worker, exceeded
its declared 2 GiB process-tree memory limit. The recorded peak was
2,414,215,168 bytes. The supervisor terminated only its owned processes and
verified closure. This is a failed resource-limited invocation, not a passing
unit suite. No candidate source files changed.

The invocation consumed 32.846 seconds of its 1,200-second total allowance.
Its 300-second per-phase limit, 4 GiB artifact ceiling and 168 GiB host free-space
reserve remain recorded. Type checks, notices, the explicit audit command and
the production build were not reached; browser checks were outside this attempt
and remain pending. Before any continuation, inspect the retained logs and
account for the consumed allowance. Do not reset this failed receipt or increase
its recorded limits. A lower JavaScript heap setting may help keep a new bounded
invocation within the same process-tree ceiling, but has not yet been tested.

The terminal receipt and both phase logs are copied to
`dashboard-build-linux-evidence` in the Windows recovery directory. The complete
isolated source and installed dependencies remain in Ubuntu under
`recovery-69dbcab9/dashboard-build-linux`. The original native and Windows Python
controllers were not restarted or changed by this check.

### Dashboard continuation closed

A separate invocation with `NODE_OPTIONS=--max-old-space-size=384` completed the
same full unit selection: **264 passed, zero failed, skipped or cancelled**.
Dependency installation, type checking, notice verification, explicit dependency
audit and production build also passed. The rebuilt static file inventory and
every file hash match the candidate bundle exactly. All 1,198 original candidate
files remain unchanged, and an independent closure confirmed every recorded
owned process has exited.

The continuation kept the same 2 GiB process-tree ceiling; the unit stage peaked
at 1,980,186,624 bytes. It charged the earlier 32.846 seconds and reserved five
seconds from the original total before launch. Its own duration was 35.250
seconds, so both invocations consumed 68.095 seconds combined. The artifact
monitor counts both retained attempt directories against the original 4 GiB
limit. The first failed receipt remains unchanged.

Evidence is in `dashboard-build-linux-heap384-evidence` and the independently
checked `dashboard-build-closure.json` under the Windows recovery directory.
Browser checks remain pending; this is not a complete dashboard release verdict.
The production build retained its existing warning for JavaScript chunks larger
than 500 kB. No bundle or source was changed to suppress that warning.

## Windows full-suite terminal reconciliation

The restored Windows 3.11 invocation is now terminal and failed: group 2 reached
its 1,800-second execution limit; its peer group 3 stopped under the declared
peer-failure rule. Groups 0 and 1 completed. Independent reconciliation verifies
all recorded process identities are absent, the original source/evidence stamps
still match, and the failed receipt remains unchanged. There were no failed
pytest phases in the retained records.

Including the 144 independently proved smoke cases, complete coverage is now
886 passing cases plus four expected hosted integration skips. Another 2,449
cases remain unproved. The limited group retains 165 complete passes and one
expected skip, with 35 unproved cases; its peer retains 177 complete passes and
one expected skip, with 23 unproved cases. No partial test is treated as passing.

This invocation consumed 2,552.141 seconds, leaving 9,222.969 seconds before the
next preparation charge from its original remaining platform allowance. A new
bounded continuation must preserve this charge and the complete phase evidence;
it must not restart the old controller or present the platform as passing.
The executable evidence is `windows-full-gate-terminal-audit.json` under the
recovery directory. All failed/skipped fixture directories remain preserved.

The browser controller is separately running under the remaining dashboard
allowance. It restores Chromium dependencies before executing the six CI browser
specifications with one worker, no retries and no reuse of an existing server.
Its record is in Ubuntu at `recovery-69dbcab9/dashboard-browser-linux/receipt.json`;
its final result remains pending.

### Disjoint Windows continuation started

`full-gate-windows-11-unproved-v2` now runs only the 2,449 independently unproved
nodes in 32 disjoint groups of at most 77 tests, using two owned workers. It
retains the same 1,800-second per-group ceiling, process memory ceilings and
artifact limits. The prior four namespaces remain included in each storage
measurement. No earlier fixture or failed receipt is deleted or rewritten.

The remaining cumulative allowance is 9,162.969 seconds after charging another
60 seconds for audit and preparation. Admission rechecked the terminal owner's
absence, all observed child identities, earlier source/evidence stamps and the
hash-pinned audit. Each new batch also pins those earlier proof files. Final
coverage must reconcile the carried 886 passes and four expected skips with
the new disjoint selection against the complete 3,339-case inventory.

The controller is `tools/rebuilt_candidate_unproved_v2.py` under the recovery
directory; preparation is recorded in
`windows-unproved-continuation-readiness.json`. This invocation remains pending.
Do not run another full platform invocation while its two workers are active.

### Browser invocation stopped at memory ceiling

The browser invocation is now terminal: system dependency installation and
Chromium download passed, but the test process tree exceeded its 2 GiB ceiling.
The supervisor stopped the owned tree and verified cleanup. The candidate copy
remains unchanged. No browser pass is claimed. The invocation consumed 347.843
seconds; combined dashboard execution time is now 415.938 seconds. The failed
receipt and all three stage logs are archived at
`dashboard-browser-linux-evidence` in the Windows recovery directory. Inspect
these measurements before a further bounded continuation; installed Chromium
and dependencies remain available and need not be installed again.

### Browser memory allowance revised for a separate attempt

The failed browser startup measured 2,295,627,776 process-tree RSS bytes within
5.356 seconds. Its log had announced the complete 121-test selection. The
record does not break RSS down by individual process, so it cannot establish
which component retained the most memory. Before the separate attempt, Windows
reported 35.23 GiB available RAM and Ubuntu reported 29.4 GiB available.

`dashboard-browser-linux-4g` uses an explicitly revised 4 GiB process-tree
allowance for the browser plus development server. This changes the provisional
resource allowance, not any test expectation. The earlier 2 GiB failure remains
unaltered. The six CI specs, one worker, zero retries, isolated server and exact
candidate source remain the same. Existing Chromium is reused.

The new attempt retains 779.062 seconds from the original 1,200-second combined
dashboard allowance after 415.938 seconds consumed and a five-second margin.
All four retained dashboard namespaces count against the same 4 GiB artifact
ceiling, and the 168 GiB host free-space reserve remains enforced. The new
attempt has started; its final browser verdict remains pending.

### Browser continuation passed and independently closed

All **121 tests across the six required CI browser specification files passed**,
with zero skipped, flaky or unexpected results and no retries. The independent
closure verified the exact specification inventory, report hash, unchanged
candidate source and absence of every recorded owned process. The invocation
took 391.549 seconds, with a measured peak tree RSS of 1,855,082,496 bytes. The
lower observed peak on this run does not erase the earlier measured overrun.

Combined dashboard execution time is 807.487 seconds, including both earlier
resource-limited attempts, within the original 1,200-second allowance. The first
failed receipts are preserved. Results are archived in
`dashboard-browser-linux-4g-evidence`; the independently checked record is
`dashboard-browser-closure.json` under the Windows recovery directory.

Together with the separately closed 264 unit tests, type checks, notices, audit
and exact production build, this closes the rebuilt candidate's dashboard CI
checks. The production-backend city/study acceptance, hosted integration and
remaining platform/native checks retain their separate requirements. This does
not admit the population registration or complete W5.

## Rebuilt production city and study acceptance started

The unchanged `scripts/city_research_acceptance.py` is running in a separate,
Git-aware copy of all 1,198 frozen candidate files. It uses the already verified
production bundle, copied locked dashboard dependencies and installed Chromium.
The script creates its own three-day world and ephemeral server, exercises both
G2 and F2 study workflows, independently imports the downloaded study bundles,
and compares the source world's complete logical dump before and after.

This is the separate 15-minute city/research CI check, not a further browser
retry. The supervisor allows 900 seconds including preparation, 4 GiB tree RSS,
8 GiB of retained artifacts and a 168 GiB host free-space reserve. Both source
copies are hash-checked on closure, and only owned processes may be stopped.
Evidence is retained under Ubuntu's
`recovery-69dbcab9/city-acceptance-recovered`. The final result is pending.

### Production city workflow passed and independently closed

The production-backend acceptance completed in 78.515 supervised seconds. Both
G2 and F2 studies have four eligible completed attempts. Their downloaded bundle
hashes and byte lengths were independently checked, and both separate bundle
imports verified. The paused 25-agent source world remains at day 3; its complete
logical dump is identical before and after the browser/study workflow.

All recorded owned processes are absent, and both 1,198-file source copies remain
unchanged. The acceptance receipt, fixture, browser evidence and logs are archived
in `city-acceptance-evidence`, with independent closure in
`city-acceptance-closure.json` under the Windows recovery directory. The separate
success/failure server-shutdown unit cases remain part of required Python matrix
coverage. This workflow result does not establish economic realism or complete
the remaining population/native/platform/hosted acceptance requirements.

## Smaller groups prepared for the remaining platforms

The unstarted Windows 3.12 and Ubuntu 3.11/3.12 full invocations now have a
prepared 32-group controller. Each still covers exactly 3,195 remaining nodes
plus its 144 independently proved smoke cases, with no omissions or duplicate
nodes. Partition reconciliation against each fresh complete collection found a
maximum of 103 selected cases per group. Concurrency remains two workers.

`tools/rebuilt_candidate_full_gate32.py` is staged with identical bytes on both
hosts. It rejects Windows 3.11, which already has its own audited continuation.
Each next platform retains its original 14,400-second total, deducts earlier
smoke/collection/diagnostic time, charges another ten seconds for this preparation,
and reserves five seconds at admission. Memory, storage, per-group limits,
source checks and phase reconciliation are unchanged. This controller supersedes
the earlier 16-group launch command for these three unstarted invocations only.

`remaining-platforms-32-readiness.json` records the executable hash and exact
partition counts. None of these invocations has started; the active Windows 3.11
continuation must finish first. Preparation is not a passing platform result.

## Native horizon completed; final campaign verification started

The native recovery reached and closed day **14,600**. Its terminal receipt is
`complete_native_horizon`, with owned cleanup verified and the preserved source
unchanged. The original and recovery execution consumed **27,631.405 seconds**
combined, below the original 28,800-second native limit. The parent process is
confirmed absent. The final database SHA-256 is
`927e461b036cb8c13c8f8c497c2bf6e83ab78142ecb73a8dfa822d047f114a59`.

The prepared `restored_campaign_verifier_v2.py --case campaign` has now admitted
the terminal run and started in `campaign-verifier-campaign-v2`. Its owner and
hash-bound specification are recorded; annual report lineage is copied into
the private verification allocation. The native source is preserved while
replay, exact comparison, export and independent readback run under their
previously declared limits. Final verification remains pending. Reaching the
horizon alone does not complete W5 or admit the population registration.

The [native horizon outcome](2026-09-11-native-horizon-outcome.md) records the
terminal demographic and execution counters, including absent birth-origin
employment and unchanged goods/equity execution counts over the final 1,736
days. These remain research limitations to retain through later work, not
reasons to rewrite the completed campaign.
# Restored hosted inputs prepared; service execution pending

The replacement input package is saved under
`data/recovery/environment-cleanup-69dbcab9/hosted-restored-inputs/`.
All 1,198 frozen candidate files were checked against their recorded byte hashes,
then independently read back from the 24,770,560-byte build-context archive.
Archive SHA-256: `dd76054a65637efafdd2a219b8c3d4044afed1c1627920da653ab0d0f9604cc1`.
The package includes the existing client and capture helper, with helper hashes,
and the eight exact collected hosted node IDs (six PostgreSQL, two S3).

The restored candidate's Compose configuration passed a read-only validation
using temporary placeholder environment values. No images were built and no
services were started. Preparation consumed 1.891 seconds, to be charged at
dispatch. This is input readiness, not hosted integration acceptance.

The original hosted supervisor still references deleted source and a superseded
64-batch audit shape. Adapt its admission to the four restored platform closures,
the new candidate manifest and this contract before execution. Keep the original
1,200-second job allowance, private service ownership, no automatic retries,
complete setup/call/teardown evidence and independently checked process closure.
Do not dispatch hosted tests before the full restored matrix is audited.


## Windows 3.11 continuation exhausted; Windows 3.12 started

The continuation closed at its cumulative time limit after 9,174.203 seconds
against its 9,162.969-second allowance, including shutdown overhead. The terminal
independent audit reconciles 2,803 complete passes, eight expected hosted skips
and 528 unproved cases. Twenty-five groups completed; groups 25 and 26 were
stopped. Group 26 stopped before collection and carries no test credit. The
audit reader was corrected to distinguish this missing collection from a
completed group; the first audit invocation wrote no acceptance artifact.

Evidence: `data/recovery/environment-cleanup-69dbcab9/windows-unproved-terminal-audit.json`,
SHA-256 `b418c0fac7d117b5d0b432563c009e20dbe3971baebf76b0ee371fc7685a08cf`.
All observed owned processes are absent and source/evidence stamps match.
Original receipts and failed fixtures remain preserved. Windows 3.11 has not
passed its full gate, and no extra allowance or automatic retry was granted.

The prepared Windows Python 3.12 full gate is now active using the previously
pinned 32-group controller (session 2504). This is a different required platform,
not a retry or substitute for incomplete Windows 3.11 coverage. Its receipt is
`data/recovery/environment-cleanup-69dbcab9/full-gate-windows-12/receipt.json`.
The campaign verifier remains active under session 7032. Linux gates follow
sequentially; hosted integration and W5 admission remain pending.


## Windows 3.12 cleanup failure; sealed passes recovered

Session 2504 is terminal after 1,223.485 seconds. Group 3 completed all 103
tests, then fixture cleanup failed with WinError 5 on a read-only Git object.
The controller stopped peer group 2, which had 74 complete passes. Groups 0 and
1 each passed 102 tests. Including the 144 verified smoke cases, independent
audit proves 525 passes and 2,814 unproved cases, with no failed test phases.

`data/recovery/environment-cleanup-69dbcab9/windows12-cleanup-terminal-audit.json`
checks source/evidence stamps, all phase records, the sealed but unreported
group 3 outcome, and absence of all owned processes. The original failed
receipt remains unchanged; group 3 cleanup may be partial and is not retried.
Remaining allowance is 12,952.218 seconds before further preparation charges.

Prepare a new versioned cleanup helper for future eligible temporary fixtures;
never edit the existing helper pinned by archived evidence. Then validate it
and select only the unproved cases for a separately recorded continuation.
The campaign verifier remains active. Neither full Windows platform has passed.


## Read-only fixture fix verified; Python 3.12 continuation started

A separate `prospective_pytest_retention_v3.py` preserves the earlier helper
and all pinned source evidence. On Windows access-denied unlink errors it
retries only a regular read-only file contained in the already approved fresh
fixture root; aliases, directories, outside paths and other permission failures
remain refusals. The complete prospective probe passed all eleven cases in
12 seconds, including a real read-only fixture, failed setup/call/teardown,
skips, interruption, changed evidence, outside links, live owners and missing
phases. Only the new 8,219-byte successful fixture was removed.

The separately recorded Python 3.12 continuation is active under session 51967.
It selects 2,814 unproved cases in 32 disjoint groups (maximum 88), carrying
525 independently verified passes. Its allowance is 12,755.218 seconds after
deducting original charges, the failed attempt, 180 seconds of preparation,
the 12-second probe and five seconds of margin. All four original namespaces
remain counted in the unchanged storage ceiling; two workers and their memory
limits remain unchanged. This does not rewrite the earlier failed receipt.

Readiness and pins: `data/recovery/environment-cleanup-69dbcab9/windows12-v3-readiness.json`.
Controller SHA-256: `c7de66b0ea2247d3e83af1b9f927594209a9d5c619de397bdfa4f43d7a8f89a2`.
Helper SHA-256: `838f40c05677c356bf93cd5da8e5c6e8a95ad1b9c9411b6e6c8d945204f495af`.
The campaign replay remains active under session 7032. Full acceptance is pending.

## Windows 3.12 continuation: independent interim audit

At 10:59 UTC, the read-only `tmp/audit-windows12-unproved-v3.py` audit
revalidated the original v2 evidence with the original validator and the new
closed groups with the v3 validator. It checked source and evidence stamps,
complete test phases, disjoint selections, and absence of closed groups' owned
processes. Five groups had closed by the snapshot: 440 new passes plus 525
carried passes, totaling 965 passes and 2,374 unproved cases, with no skips yet.

The saved snapshot is
`data/recovery/environment-cleanup-69dbcab9/windows12-v3-first4-audit.json`;
its filename was chosen before the fifth group finished. The receipt snapshot
hash is `8d34a9439a79604a1e21e961e0cdfb07960710ff27049653569216e9ceea1109`.
The controller remained live and had consumed 1,390.688 of 12,755.218 seconds.
This is interim evidence, not full platform admission. The audit deleted no
fixtures and dispatched no tests. Run it without `--partial` after successful
termination to require complete coverage and terminal ownership checks.

## Windows 3.12 closed; Ubuntu full gate started

At 13:24 UTC, the terminal independent audit verified 3,331 complete passes,
eight declared hosted-service skips, and zero unproved cases. All 32 groups
closed; the controller and closed groups' owned processes were absent. Source
and evidence stamps passed. The continuation consumed 10,121.266 seconds of
its 12,755.218-second allowance. Earlier failed receipts remain preserved.

Evidence: `data/recovery/environment-cleanup-69dbcab9/windows12-v3-terminal-audit.json`.
Receipt SHA-256: `3ba78fab0e07ca4dbe8f7f72bb95fb35ecb58c5ce0dcb9166e0b9f5dc832725d`.
This closes this platform's coverage, not W5 or full release admission.

Ubuntu Python 3.11.15 was live-verified. Its prepared 32-group full gate was
started under session 82105 after Windows session 51967 terminated. The
campaign verifier remains active under session 7032; its latest observed
replay progress was day 9,000 of 14,600. Linux 3.12 and hosted verification
remain pending, as do the previously documented Windows 3.11 coverage gaps.

The new read-only `tmp/audit-restored-linux-platform.py` rechecks the original
144 smoke cases and each completed full-suite group's preserved phases, source
stamps and process closure. At 13:27 UTC its initial Linux 3.11 audit confirmed
246 passes (144 smoke plus 102 in group 0), zero skips and 3,093 cases still
unproved. The controller remained active. Evidence is Ubuntu recovery file
`linux11-initial-audit.json`; plan SHA-256 is
`c864b2f220817a7de2d7587bd370e24b88041fd5f59b256e8bf99352ce8fb5fc`.
Run the same auditor without `--partial` at successful termination, using a
new output filename; full coverage and terminal ownership checks are required.

## Verification interruption observed at 13:53 UTC

Both tool sessions (82105 and 7032) became unavailable. Direct process checks
found neither the original Linux controller (363) nor Windows controller
(27124) or replay writer (37860). Ubuntu's process list showed a fresh boot.
The stale Linux receipt still says `running`; this is not evidence of a live
controller. No campaign terminal receipt was written. Neither job was restarted.

The read-only Linux interruption audit revalidated 858 passes, including the
144 smoke cases and seven completed full-suite groups, with zero skips and
2,481 cases unproved. Ubuntu recovery file `linux11-interruption-audit.json`
records receipt snapshot SHA-256
`e5c3cb664ed84a9ddcbbd035d36e33693159c5853cd52006f9ffdc820e462473`.
Groups 7 and 8 were recorded as active at interruption and receive no credit
from this audit. Their fixtures and phase evidence remain preserved.

Replay's last saved checkpoint was day 9,700 of 14,600 after 18,183.281
seconds. This is partial progress, not exact replay proof. Before continuation,
inspect retained databases and phase evidence, establish a clean boundary,
and carry original time and storage charges forward. W5 remains unadmitted.

### Interrupted replay copy inspected

`tmp/inspect-interrupted-replay.py` copied the database, WAL and SHM into
`data/recovery/environment-cleanup-69dbcab9/campaign-interruption-inspection`
after confirming original controller/writer absence. SQLite quick-check passed
on that private copy in 20.046 seconds; hashes, sizes and timestamps verified
the original three files remained unchanged. `inspection.json` preserves the
original file stamps and inspection results. The copy contains 192,393 recorded
calls. Direct read-only inspection of its run metadata found completed tick
9,736, active tick 9,737, phase `MORNING`—later than the last progress report.

Resumability is not yet proven. The existing verification helper initializes
an empty replay and asserts tick zero; it cannot simply be rerun on this copy.
Continuation must restore the persisted partial-phase state and recorded-call
consumption correctly, and prove that behavior before resuming this artifact.
The original interrupted files remain retained; no replay was dispatched.

The subsequent read-only `tmp/audit-interrupted-replay-prefix.py` compared all
192,393 non-operational replay calls, in order, with the original source prefix.
Every request, response, cache key, agent, role, tick, provider, model, token
count, cached flag and cost matched exactly. Source IDs ended at 192,393.
`campaign-interruption-inspection/call-prefix-audit.json` records the result
and identity-map SHA-256
`9f06599687ba7cc6bd8ddf1ac17ce35873661b3691800a6c7ff7da30e454ff49`.
The audit took 20.579 seconds and dispatched no simulation decisions.

This supports rebuilding the consumed source-ID set from persisted records.
It does not establish original in-memory match-mode counters, nor prove resumed
execution. A bounded interrupted-versus-uninterrupted replay test must check
partial MORNING recovery, PRNG restoration, duplicate-key handling and final
logical equality before the long campaign continuation is admitted.

The bounded baseline recovery diagnostic now reproduces a failure on the
three-day smoke source: after committing a call in day 2 MORNING, closing,
restoring PRNG state and rebuilding consumed IDs, replay pauses with missing
recorded responses rather than completing day 2. Final diagnostic evidence:
`replay-resume-baseline-probe-v3/result.json` (0.844 seconds). This is a saved
negative result, not a passing recovery test. The replay branch bypasses the
gateway's normal durable-call lookup, so rebuilding IDs alone is insufficient.

The first diagnostic was explicitly stopped because its loop retried a paused
day; the second exposed an unclosed source cursor during Windows snapshot
cleanup. Both artifacts remain retained. The final diagnostic stops on the
first non-advancing day and closes that cursor. No campaign data was resumed
or changed. Next: implement and verify durable reuse within replay recovery,
including event-reference localization and no duplicate logging/consumption.

The explicit Semantics 20 recovery adapter in `tmp/replay_recovery.py` now
validates the persisted call prefix, rebuilds consumed IDs and reuses committed
MORNING responses with schema validation and event-reference localization.
Durable reuse does not insert another call or charge its cost again. Original
match-mode counters are marked unavailable rather than reconstructed as exact.
Frozen engine/candidate sources remain unchanged.

`replay-resume-recovery-probe-v1/result.json` records successful recovery to
day 3: 27 calls restored, nine durable responses reused, zero left unused.
The independent full logical comparison in that directory's `exact-proof.json`
passed with no differences and matching hash
`f0bdafbb7ac510d3ac1e30b202c915f6e81d51ea5c472b5dfaaace3aa39c6dd6`.
The comparator used a recovered private source copy because the small source
had SQLite sidecars; no source sidecars were manually removed. Initial comparison
setup failures (closed-source requirement and missing scratch directory) were
corrected before the successful comparison. Campaign continuation remains
pending duplicate-key/rejection coverage and a bounded continuation supervisor.

Recovery boundary checks now pass: `tmp/test_replay_recovery.py` ran five
tests successfully (duplicate cache-key occurrence order, schema rejection,
transform rejection, changed-prefix atomic refusal, and other-tick isolation).
Restoration now stages all consumed identities before installing any state;
a mismatch in a later row leaves the gateway untouched.

After that change, `replay-resume-recovery-probe-v2/result.json` again reached
day 3 (27 restored calls, nine reused responses, zero remaining). Its independent
`exact-proof.json` again reports exact logical equality with no differences.
Adapter SHA-256:
`fce42d7825d717f74a8a55308190e07189857c646bfaa5f5f5286c189e1fb17b`.
The long campaign has not been resumed. Next required work is an owned
continuation supervisor that preserves the interrupted source files, carries
elapsed time/storage charges and recovery evidence, stops on a non-advancing
day, and distinguishes restored consumption from newly observed match modes.

Campaign replay continuation is now dispatched under session 61765 using
`tmp/continue-interrupted-campaign-replay.py`; its independent namespace is
`campaign-replay-continuation-v1`. The supervisor charges 18,393.662477 seconds
through confirmed original-process absence plus 600 seconds for recovery and
preparation, leaving 9,806.337523 seconds from the original replay allowance.
It checks 16 GiB process memory, 16 GiB per private/replay allocation, 48 GiB
combined work including the old case and inspection copy, and 40 GiB free space.
Original interrupted DB/WAL/SHM hashes and timestamps are revalidated.

The child has started; a terminal result is not yet available. Recovery uses
fresh private database copies, the original native runtime/source, and the
tested adapter. The runner stops on a non-advancing day and does not retry.
Completion here proves only replay execution; exact comparison, export,
readback and life-course checks remain required. Linux verification remains
interrupted and needs a separate continuation carrying its prior coverage.

Linux 3.11 continuation is dispatched under session 17044, namespace
`full-gate-linux-11-recovery-v2`. A fresh independent revalidation confirmed
858 carried passes. The controller selects only the remaining 2,481 cases,
distributed across 32 nonempty groups; the prepared v1 was never dispatched
because retaining the old partition positions would create empty groups.
All nine original namespaces remain counted for storage, including the two
interrupted groups. It charges 1,704.83 seconds through observed original-owner
absence plus 60 seconds preparation, in addition to the earlier smoke/probe
charges. Two workers and existing resource ceilings remain unchanged.

Ubuntu readiness: `linux11-recovery-readiness-v2.json`; controller SHA-256:
`bb61dfbbb534f06043fc6729af05bc0d192587360d4638be35c535da2fa5318e`.
Independently audit the continuation's closed phases and carried evidence at
termination before starting Linux 3.12. Campaign session 61765 also remains
live: it restored 192,393 source identities and found four persisted MORNING
responses available for durable reuse. Neither continuation is yet complete.

Independent partial audit at 15:01:57 UTC: `linux11-recovery-partial-audit.json`
revalidated the 858 carried passes and 18 closed continuation groups, totaling
2,257 passes and four declared hosted skips; 1,078 cases remain unproved at
that snapshot. Source hashes, collection identities, complete test phases,
closed process evidence and disjoint coverage were checked by
`tmp/audit-linux11-recovery-v2.py`. This is not terminal platform approval.
The two live sessions remain 17044 (Linux tests) and 61765 (replay). Replay
progress reached day 10,800; exact comparison and downstream verification
remain pending. Ubuntu is installed and the Python 3.11/3.12 runtimes are
available; another Ubuntu installation is unnecessary.

Recovered-replay downstream analysis is now implemented in
`tmp/analyze-recovered-campaign.py` (SHA-256
`33229ad32e4b7cdd1f2ebeee555f1203e8ce2d5e7c4f0891da9d5fbf87019c44`).
The smoke invocation completed under session 29984, exit 0. Independent
closure `recovered-analysis-smoke-closure.json` confirms all owned processes
closed, protected source/replay hashes unchanged, exact comparison with no
differences, export/readback of 193 tables and 2,752 rows, and passing
life-course invariants. Total supervisor time was 45.485 seconds.

The full campaign analysis has not started. Its entry point requires a
terminal successful replay continuation, original replay time-accounting
closure, and this same controller smoke receipt. It reuses the closed replay
read-only, creates a private standalone source copy, retains the previous
interrupted namespaces in the combined 48 GiB storage accounting, and keeps
the original 7,200/7,200/3,600-second comparison/export/readback ceilings.
No automatic retries or population admission are inferred. Full dispatch:
`.venv/Scripts/python.exe -B -u tmp/analyze-recovered-campaign.py --case campaign`
only after session 61765 terminates successfully and its receipt is audited.

Windows 3.11 supplemental coverage is prepared, not dispatched.
`windows11-supplemental-readiness.json` records successful read-only preflight
of the actual controller admission statements, including selected inventory,
source hashes, absent prior owners, tools and disk capacity. Session 72141
closed with exit 0; it ran no pytest workers and created no gate namespace.
The prepared controller is `tools/windows11_supplemental_coverage_v1.py`,
SHA-256 `1ed72f9a01c79f9ce7c82de2e65c970c0ed7b8239700eddde918339d468543a6`.

This separate prospective allowance selects the 528 unproved cases in 14
nonempty groups, two workers, 1,800 seconds per group, 7,200 seconds total.
The original Windows time-limit failures and their resource accounting remain
unchanged; this result alone cannot declare the original platform gate passed.
Old namespaces remain counted in the 64 GiB logical/allocated test ceiling;
104 GiB free-space floor and 2 GiB per-worker memory ceiling remain enforced.
No original fixtures are eligible for its cleanup. Dispatch is sequenced after
both Linux platform jobs close; no supplemental workers are running now.

`tmp/audit-windows11-supplemental.py` is prepared for independent terminal
phase/source/process verification; syntax checked, no results claimed. After
new coverage closes, revalidate the earlier 2,803 passes/eight hosted skips
and combine the disjoint inventory explicitly, preserving prior failed gates.

The restored hosted supervisor is prepared in `tmp/run-restored-hosted-gate.py`,
SHA-256 `ef44bfa8ab5579148479b1b1bf9cf8e716b5d9d4c787cd4e8997511d532d8e15`.
`restored-hosted-supervisor-readiness.json` records successful source/archive
preflight against all 1,198 frozen candidate files and the saved contract.
Its original private Docker ownership, labels, credentials, nonroot build/CLI
smoke, eight PostgreSQL/S3 cases, retained evidence and owned-container-only
stop logic are retained. It charges the saved 1.891-second preparation plus
60 seconds supervisor preparation against the 1,200-second deadline.

The new phase validator requires exactly one passing setup/call/teardown per
expected test. Four focused checks in `tmp/test_restored_hosted_phases.py`
passed, including rejection of a duplicate setup replacing a teardown, an
unexpected test identity, and a failed teardown after a passing call.
These are runner checks; no hosted integration pass is claimed. No Docker
services have been built, created, started, stopped or deleted by this work.

Full dispatch still requires an independently closed restored matrix artifact:
status `restored_matrix_coverage_independently_closed`, source manifest hash,
3,339 cases per platform, all owned processes absent, source and phase evidence
verified, prior failed attempts preserved, and four platform entries named
`windows-11`, `windows-12`, `linux-11`, `linux-12`. Each entry must show
3,331 passes/eight declared hosted skips/zero unproved cases and pinned
evidence hashes. Build that artifact from terminal audits, not progress files.
The supplemental Windows coverage can combine disjoint closed results while
preserving the earlier time-limit failures. Hosted invocation, once admitted:
`.venv/Scripts/python.exe -B -u tmp/run-restored-hosted-gate.py --audit <matrix-closure-path>`.

Latest observed campaign progress is day 11,200. Sessions 17044 and 61765
were both polled and still running after the hosted preparation.

Full platform union auditor implemented: `tmp/audit-restored-platform-union.py`.
Unlike a receipt-count summary, it reconstructs credit from every original
smoke and closed group, validating complete phases, source and evidence hashes,
absent process identities and disjoint credited nodes. It includes the sealed
Windows 3.12 group recovered after its fixture-cleanup exception.

Windows 3.12 union verification ran successfully (session 85081, exit 0).
`windows12-platform-union-closure.json` proves 3,331 passes/eight hosted skips,
zero unproved, source content identity
`6d3cbf1a0648d82bf777f683cdcf47555545be7d85ffe8733865a22fad3045a0`.
Prior failed attempts remain recorded. No full release claim is made.

For each remaining platform, execute this auditor with its native runtime and
`--minor 11|12 --output windows11-platform-union-closure.json` (Windows) or
`linux11-platform-union-closure.json` / `linux12-platform-union-closure.json`
(Linux). It requires the latest run to be terminal and fully covered.
Then `tmp/close-restored-matrix.py` combines all four native union closures,
requires identical source content, and revalidates raw closed-phase evidence.
The assembler uses the Ubuntu WSL share for Linux proof paths; syntax checked,
not yet run because three closures remain unavailable. No aggregate matrix
verdict or hosted admission has been written.

Linux 3.11 recovery session 17044 has terminated successfully (exit 0): all
32 groups closed, 4,994.479616 seconds in the continuation. Independent
`linux11-recovery-terminal-audit.json` and `linux11-platform-union-closure.json`
prove 3,331 passes, eight declared hosted skips and zero unproved cases.
Terminal receipt SHA-256:
`74dac50b60648b6024e8bc8fb9313ccc9855e8675f4b3afd2d88874c031ce74a`.
The union revalidates original smoke/closed groups plus all continuation groups,
source identity and absent owners. Its source identity matches Windows 3.12.
No earlier interruption or artifact is erased.

Linux 3.12 has now been dispatched under session 4057 using the original
`tools/rebuilt_candidate_full_gate32.py` in Ubuntu and the restored 3.12.13
runtime. Preflight confirmed the completed Linux 3.11 union audit, an absent
new namespace, the pinned controller hash and at least 168 GiB free space.
Receipt: Ubuntu `full-gate-linux-12/receipt.json`. Two workers select 3,195
unproved cases in 32 groups, carrying the 144 validated smoke cases under
the existing platform limits. No terminal Linux 3.12 result exists yet.
The next full test run remains the Windows 3.11 supplemental selection;
replay session 61765 is still active.
# Replay continuation exhausted its allowance — 2026-09-11 16:54 UTC

Session `61765` closed with exit 1. The owned replay child stopped with exit 15
and `remaining_replay_allowance_exhausted`; its last progress report is day
13,000 of 14,600. This is an incomplete verification result, not a failure of
the already completed native horizon. Do not infer the interrupted database's
exact final tick from its coarser progress report.

`tmp/audit-replay-allowance-exhaustion.py` completed with exit 0 in 19.797 seconds.
Its saved `campaign-replay-allowance-terminal-audit.json` independently confirms
parent/child process absence, all pinned inputs and frozen native files, the
original interrupted replay's preservation, and the unchanged native source
SHA256 `927e461b036cb8c13c8f8c497c2bf6e83ab78142ecb73a8dfa822d047f114a59`.
It did not open or checkpoint the newly interrupted replay database.

Total charged time is 28,800.678477 seconds against the original 28,800-second
allowance, including 600 seconds of preparation and 0.678477 seconds of shutdown
overshoot. Preserve the original failure and all attempt artifacts; no automatic
retry or allowance extension has been made. The prepared full comparison,
export and readback runner was not dispatched because terminal replay success
is absent. W5 remains unadmitted. Linux 3.12 session `4057` is still active;
its platform closure and the prepared Windows 3.11 supplemental coverage remain
independent next actions.
# Independent native-source export — 2026-09-11

After the replay timeout, source-only export and readback are being executed
separately. The harness's `analysis_stage` opens the native source for these
stages; only its `compare` branch needs a replay database. This separation does
not establish replay equivalence or population acceptance.

`tmp/export-completed-native.py` SHA256
`e3dafb5be732c1a2643f654d7cf1fbe64f4ad3139a68eeefa69ff80616f0c329`
preserves the frozen analysis source, guarded child processes, source hashes and
sidecar stamps. It copies the sealed native database and never opens either
interrupted replay. The original comparison/replay runner remains unchanged.
The small rehearsal (session `82962`, exit 0) independently closed with 193
tables / 2,752 rows, matching export/readback bundle hashes and passing
life-course invariants; see `native-source-export-smoke-closure.json`.

Full native-source export session `65419` has started in
`native-source-export-campaign-v1`. Its original unused export/readback allowances
are 7,200/3,600 seconds, 16 GiB process memory, a 40 GiB free-space floor and
48 GiB combined retained work including both replay attempts and inspection.
Before the private source copy, retained campaign work was 38,997,746,272 bytes,
leaving 12,541,861,280 bytes within that combined cap. No retries, replay time
extension, deletion or W5 admission are implied. Poll this session to completion
and independently inspect its result. Linux 3.12 session `4057` also remains live.

The user installed Graft and updated `AGENTS.md`, `.gitignore`, Codex MCP config
and `opencode.json`. The updated routing instructions were read; `graft map`
worked in this session with 668 indexed files. A new session is unnecessary for
CLI graph queries. Preserve these user changes and the frozen validation source.
# Linux 3.12 closed; Windows supplemental started — 2026-09-11 17:22 UTC

Linux session `4057` exited 0 after all 32 groups in 6,181.637774 seconds.
`tmp/audit-restored-platform-union.py --minor 12 --output linux12-platform-union-closure.json`
exited 0 and independently verified 3,331 passes, eight declared hosted skips,
zero unproved nodes, original source identity and complete test-phase evidence,
with every owned process absent. Its source-content SHA256 is
`6d3cbf1a0648d82bf777f683cdcf47555545be7d85ffe8733865a22fad3045a0`,
matching Linux 3.11 and Windows 3.12. This is not full release acceptance.

Windows 3.11 supplemental session `69528` has started using the previously
prepared `windows11_supplemental_coverage_v1.py`, verified SHA256
`1ed72f9a01c79f9ce7c82de2e65c970c0ed7b8239700eddde918339d468543a6`.
It covers the remaining 528 tests within its separately declared 7,200-second
allowance; the original Windows gate's timeout remains recorded as a failure.
On terminal success, run `tmp/audit-windows11-supplemental.py`, then the Windows
3.11 platform-union audit and `tmp/close-restored-matrix.py` before the prepared
hosted integration runner. Native-source export session `65419` is still live.
Replay remains incomplete after its exhausted allowance; W5 remains unadmitted.
# Native export independently closed — 2026-09-11 17:47 UTC

Session `65419` exited 0. Export and independent readback agree on 193 tables,
4,348,393 rows and bundle SHA256
`95827445703c5355d195e8633d02a1da40573e6194db525c84eda91345c2952c`.
Life-course invariants passed. The independent closure verifies source/sidecar
preservation, controller and input hashes, process absence and resource limits.
See `native-source-export-campaign-closure.json` in the recovery directory.
Total runtime was 2,857.672 seconds; retained work was 45,178,381,681 bytes.
Full replay remains incomplete after its timeout; these checks do not grant W5
acceptance. Windows supplemental session `69528` remains in progress.
